// 木構造の情報を出力
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, id, k, c[100000];

    cin >> n;
    for(int i=0; i<n; i++){
        cin >> id >> k;
        for(int j=0; j<k; j++){
        }
    }
    // 出力情報
    // 次数は今回の場合、子の数
    //node: id, parent = p, depth = d, type, [c1, ... , ck]
}